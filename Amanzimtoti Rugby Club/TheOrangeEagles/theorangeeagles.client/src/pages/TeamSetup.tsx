import React from 'react'
import { Typography } from '@mui/material'

const TeamSetup: React.FC = () => {
  return <Typography variant="h2" color="primary">Team Setup</Typography>
}

export default TeamSetup